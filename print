#include <iostream>
using namespace std;

int main() {
    long long n,x;
    cin >> n >> x;
    bool found = false;
    for (int i = 1 ; i <= n ; i++){
        if (i % x == 0){
            cout << i << endl;
            found = true;
        }
    }
    if (!found){
        cout << -1 << endl;
    }
    return 0;
}
